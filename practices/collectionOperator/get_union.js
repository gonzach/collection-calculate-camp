'use strict';

function get_union(collection_a, collection_b) {
  //List<Integer> unionA = collection_a.stream.collect(Collectors.toList();
 // List<Integer> unionB = collection_b.stream.collect(Collectors.toList();
 const res = [];
 return res = Arrays.stream(collection_a);
 
//return Arrays.stream(collection_a).collect(Collectors.toList());
  //return collection_a.stream.collect(Collectors.toList()).addAll(collection_b.stream.collect(Collectors.toList()));
  //List<Integer> distinctUnion = unionA.stream().distinct().collect(Collectors.toList());
  //return distinctUnion;
}

module.exports = get_union;

