'use strict';

function spilt_to_zero(number, interval) {
  let splitResult = [];
  for(let num = number; num <= 0; num++){
    console.log(num);
    }
    return splitResult;
}

module.exports = spilt_to_zero;
