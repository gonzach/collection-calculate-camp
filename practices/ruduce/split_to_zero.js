'use strict';

function spilt_to_zero(number, interval) {
  let splitResult = [];
  let num = 0;
  for( num = number; num <= 0; ){
    console.log(num);
    num = num - interval;
    }
    
}

module.exports = spilt_to_zero;
